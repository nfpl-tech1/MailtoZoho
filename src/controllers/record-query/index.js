/**
 * Record Query Controllers Index
 */

const emailController = require('./email.controller');
const zohoController = require('./zoho.controller');

module.exports = {
    emailController,
    zohoController
};
